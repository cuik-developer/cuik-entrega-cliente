name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

# TODO: Configure for Dokploy deployment

jobs:
  build-and-push:
    name: Build Docker Image
    runs-on: ubuntu-latest
    # TODO: Only run after CI passes
    steps:
      - uses: actions/checkout@v4

      # TODO: Configure Docker registry (GHCR, Docker Hub, or private)
      # - uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}

      # TODO: Build and push Docker image
      # - uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     file: docker/Dockerfile
      #     push: true
      #     tags: ghcr.io/${{ github.repository }}:latest

      - run: echo "Deploy workflow placeholder — configure Docker registry and Dokploy webhook"

  # TODO: Add deploy step that triggers Dokploy webhook
  # deploy:
  #   name: Deploy to Dokploy
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   steps:
  #     - run: curl -X POST ${{ secrets.DOKPLOY_WEBHOOK_URL }}
